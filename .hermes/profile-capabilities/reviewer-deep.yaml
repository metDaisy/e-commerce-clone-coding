profile: reviewer-deep

skills:
  additional_disabled:
    - simplify-code
    - systematic-debugging

approvals:
  mode: manual

mcp:
  allowed_servers:
    - semble-mcp
    - codebase-memory
  filters:
    codebase-memory: codebase_read
    semble-mcp: semble_read
